# Day 002. Git의 모든 것 — 내부 구조부터 생태계까지

> 🗓️ 2026-02-25  
> 🏷️ Tags: `Git` `버전관리` `스냅샷` `델타압축` `해시` `GitHub` `GitLab`

---

## 🤔 질문

Git은 파일을 어떻게 저장하고, 용량은 어떻게 관리할까?  
GitHub과 GitLab은 뭐가 다르고, Git은 누가 만들고 관리하는 걸까?

---

## 📌 핵심 요약

Git은 커밋할 때마다 **프로젝트 전체의 스냅샷(snapshot)** 을 저장한다.  
해시 기반 중복 제거와 델타 압축으로 용량을 효율적으로 관리하며,  
GitHub과 GitLab은 이 Git 위에 **웹 협업 기능을 얹은 플랫폼**이다.

---

## 🔍 Part 1. Git의 내부 구조

### 1-1. 스냅샷이란?

스냅샷은 **"그 시점에 프로젝트의 모든 파일이 어떤 상태였는지"를 통째로 기록한 것**이다.

카메라로 비유하면, 방 안에 물건 10개가 있을 때 매일 사진을 한 장씩 찍는 것과 같다.  
사진 한 장에는 그 순간 방의 모든 물건 상태가 담긴다. 이게 스냅샷이다.

반면 이전 세대 버전 관리 도구(SVN 등)의 **델타(delta) 방식**은 사진을 찍지 않고  
메모장에 "오늘 의자를 옮겼고, 컵을 추가했다"라고 변경 내역만 기록하는 것이다.  
특정 시점을 보려면 초기 버전부터 변경 이력을 순서대로 재생해야 했다.

Git이 스냅샷을 택한 덕분에 생기는 이점:

- **브랜치 전환이 빠르다** — 해당 커밋의 스냅샷을 통째로 참조하면 되므로 델타 재계산 불필요
- **분산 환경에 유리하다** — 각 커밋이 독립적인 스냅샷이라 네트워크 없이도 온전한 복원 가능
- **merge가 수월하다** — 두 스냅샷을 직접 비교하므로 긴 델타 체인 합산보다 충돌 판단이 단순

---

### 1-2. 해시 = 파일을 찾아가는 고유 주소

Git은 파일을 저장할 때 원래 파일명 대신 **내용의 SHA-1 해시값을 파일명으로** 사용한다.  
해시는 파일 내용 자체가 아니라, 원본을 찾아가기 위한 **고유 주소** 역할이다.

도서관에 책을 맡기면 사서가 고유한 청구기호를 붙여주는 것과 같다.  
청구기호만으로는 내용을 알 수 없지만, 그 번호로 서가에 가면 원본 책을 찾을 수 있다.

```
파일 내용: "hello world"
     ↓ SHA-1 해시 함수 적용
해시값: 2aae6c35c94fcfb415dbe95f408b9ce91ee846ed
```

이 해시값이 `.git/objects/` 폴더 안에서 실제 파일을 저장하는 경로가 된다.

```
.git/objects/
  └── 2a/
       └── ae6c35c94fcfb415dbe95f408b9ce91ee846ed  ← 원본 내용 저장
```

같은 내용은 항상 같은 해시가 나오므로, Git은 **"이 해시 이미 있네? 저장 안 해도 되겠다"** 라고 판단할 수 있다. 이것이 중복 제거의 원리다.

---

### 1-3. 스냅샷의 내부 구조: commit → tree → blob

커밋을 하면 Git은 내부적으로 세 종류의 객체를 만든다.

```
commit (커밋 객체) ─ 누가, 언제, 왜 커밋했는지 기록
  └── tree (디렉토리 구조) ─ "어떤 해시가 어떤 경로에 있는가" 지도
        ├── blob ← README.md  (파일 전체 내용의 해시)
        ├── blob ← index.js   (파일 전체 내용의 해시)
        └── tree ← src/
              ├── blob ← app.js
              └── blob ← utils.js
```

- **blob**: 파일 전체 내용을 해시값 이름으로 저장한 객체
- **tree**: 어떤 blob이 어떤 경로에 있는지 기록하는 지도
- **commit**: tree를 가리키며 메타 정보(작성자, 메시지 등)를 담은 객체

파일을 수정하지 않고 커밋하면, 해당 파일의 해시는 변하지 않으므로  
**이전 blob을 그대로 참조**한다. 실제로 새로 저장되는 건 변경된 파일의 blob뿐이다.

```
우리가 보는 구조:            Git이 실제로 저장하는 구조:

src/                        .git/objects/
├── index.js                ├── 2a/ae6c35...  ← index.js 전체 내용 (압축)
├── app.js                  ├── 7b/f3a1e2...  ← app.js 전체 내용 (압축)
└── README.md               └── e4/52d1b8...  ← README.md 전체 내용 (압축)

                            .git/objects/
                            └── (tree 객체)    ← "2aae6c...는 src/index.js" 라는 지도
```

---

### 1-4. 용량 관리 전략: 4단계

스냅샷 방식은 매 커밋마다 전체 파일을 저장하므로 용량이 걱정될 수 있다.  
Git은 이를 4단계로 해결한다.

**1단계 — zlib 압축 (기본)**

모든 blob은 저장 시 zlib으로 압축된다.  
텍스트 기반 코드 파일은 압축률이 높아 원본 대비 절반 이하로 줄어드는 경우가 많다.

**2단계 — 해시 기반 중복 제거 (자동)**

같은 내용 → 같은 해시 → 한 번만 저장.  
파일 100개 중 1개만 바꿔도 새로 저장되는 건 1개뿐이다.

**3단계 — Packfile + 델타 압축 (핵심)**

loose object가 일정량 쌓이면 Git은 이를 하나의 Packfile로 묶으며 델타 압축을 적용한다.

```
[loose objects - 초기 상태]
blob A (v1) → 파일 전체 100KB
blob A (v2) → 파일 전체 100KB
blob A (v3) → 파일 전체 100KB
합계: 300KB

[packfile로 압축 후]
blob A (v3) → 파일 전체 100KB  (최신 = base)
blob A (v2) → "v3과의 차이" 2KB  (델타)
blob A (v1) → "v2와의 차이" 3KB  (델타)
합계: 105KB
```

**4단계 — Garbage Collection (`git gc`)**

어떤 커밋에서도 참조하지 않는 고아 객체를 주기적으로 정리한다.

```
전체 흐름:

파일 저장 → zlib 압축 → 해시로 중복 제거 → loose object 저장
                                                ↓ (일정량 쌓이면)
                                           Packfile + 델타 압축
                                                ↓ (고아 객체 발생 시)
                                           Garbage Collection
```

---

### 1-5. 델타 압축 상세

델타 압축은 **원본(base)과 대상의 차이만을 기록**하는 방식이다.  
틀린 그림 찾기에서 "원본 한 장 + 다른 부분 메모"로 저장하는 것과 같다.

Git의 델타 압축은 줄 단위가 아니라 **바이트 단위**로 동작하며 두 가지 명령어로 구성된다.

```
COPY  → base에서 offset 위치부터 length만큼 그대로 복사
ADD   → 새로운 바이트를 삽입
```

실제 예시:

```
base (원본):   "The quick brown fox jumps over the lazy dog"
target (수정): "The quick brown cat jumps over the happy dog"

delta 명령:
  COPY  offset=0,  length=16   → "The quick brown "
  ADD   "cat"                   → 새로 삽입
  COPY  offset=19, length=16   → " jumps over the "
  ADD   "happy"                 → 새로 삽입
  COPY  offset=39, length=4    → " dog"
```

바이트 단위를 사용하는 이유는, 이미지나 바이너리 파일에는 "줄"이라는 개념이 없기 때문이다.  
바이트 단위면 텍스트든 바이너리든 같은 방식으로 압축할 수 있어 범용성이 높다.

---

### 1-6. 델타 체인과 과거 버전 복원

Packfile에서 **가장 최신 버전이 base(원본), 과거 버전이 델타**로 저장된다.  
과거 버전을 복원하려면 최신에서 출발해 델타를 순차 적용한다.

```
v1을 복원하고 싶다면:

1. v3 원본을 꺼낸다          → v3 확보
2. v2 델타를 v3에 적용한다    → v2 복원
3. v1 델타를 v2에 적용한다    → v1 복원
```

과거로 갈수록 거쳐야 할 단계가 많아지므로, Git은 **델타 체인 깊이를 기본 50으로 제한**한다.  
체인이 길어지면 중간에 새로운 base를 삽입하여 끊어준다.

```
v100 ← base (원본)
 ...
v51  ← 델타
v50  ← 새로운 base (원본)  ← 체인 끊김
v49  ← 델타
 ...
v1   ← 델타
```

이 구조 덕분에 아무리 오래된 버전이라도 **최대 50단계 이내에서 복원**할 수 있다.

---

## 🔍 Part 2. Git 생태계

### 2-1. GitHub vs GitLab

Git은 로컬에서 동작하는 **도구**이지 웹 서비스가 아니다.  
여러 사람이 협업하려면 코드를 공유할 중앙 서버가 필요한데, 이 빈자리를 채운 것이 GitHub과 GitLab이다.

**GitHub (2008년)**

- 코드 호스팅 + 협업에 집중하며 Pull Request를 대중화
- 2018년 Microsoft가 인수
- 오픈소스 생태계의 사실상 표준 플랫폼
- 필요한 기능은 외부 도구(Jenkins, CircleCI 등)와 연동하는 **생태계 방식**
- 클로즈드 소스

**GitLab (2011년)**

- 코드 작성부터 배포까지 전체 과정을 하나의 플랫폼에서 해결하는 **올인원 방식**
- CI/CD를 초기부터 핵심 기능으로 내장 (`.gitlab-ci.yml` 하나로 파이프라인 구성)
- 오픈소스 Community Edition 제공 → 자체 서버에 무료 설치 가능
- 보안이 중요한 기업·공공기관에서 설치형으로 많이 선택

> 핵심 차이는 "최고의 코드 협업 허브" vs "개발 전체 주기를 하나로"라는 **철학의 차이**다.

현업에서는 오픈소스·포트폴리오는 GitHub, 사내 프로젝트는 GitLab 설치형에 두는 조합이 흔하다.

---

### 2-2. GitHub 이전에는 Git을 어떻게 사용했을까?

2005~2008년, GitHub이 없던 시절의 Git 협업 방식은 지금과 많이 달랐다.

**이메일 패치**

가장 널리 쓰인 방식이며, 리눅스 커널 개발은 지금도 이 방식을 사용한다.

```
1. 코드를 수정한다
2. git format-patch로 패치 파일을 만든다
3. 메일링 리스트에 이메일로 보낸다
4. 관리자가 메일을 읽고 리뷰한다
5. 괜찮으면 git am으로 패치를 적용한다
```

지금의 Pull Request 역할을 이메일이 대신했다.

**SSH 직접 접속**

서버에 bare repository를 만들어두고 팀원들이 SSH로 push/pull 했다.

```bash
# 서버에 bare repo 생성
git init --bare /srv/git/project.git

# 팀원이 clone
git clone user@server:/srv/git/project.git
```

명령어는 지금과 같지만, 웹 UI 없이 터미널에서 `git log`, `git diff`로 리뷰했다.

**Git 데몬**

공개 프로젝트는 `git daemon`을 띄워 `git://` 프로토콜로 누구나 clone 가능하게 했다.

**gitweb**

커밋 히스토리를 브라우저에서 볼 수 있는 간단한 웹 UI.  
지금의 GitHub과 비교하면 커밋 로그 뷰어 수준이었다.

> GitHub은 이 모든 불편함을 **웹 브라우저 하나로** 해결했다.  
> Fork, Pull Request, Issues — 이것이 오픈소스 참여를 폭발적으로 늘린 핵심이다.

---

### 2-3. Git은 지금도 업데이트된다

Git은 2005년 첫 버전 이후 현재까지 활발하게 릴리즈되고 있다.  
단, **핵심 구조(스냅샷, 해시 객체, DAG 커밋)는 거의 변하지 않았다.**

업데이트가 이루어지는 세 가지 영역:

**성능 최적화** — 대규모 저장소(모노레포)를 위한 sparse checkout, fsmonitor 등.  
Microsoft가 Windows 소스코드(수백 GB) 관리를 위해 직접 기여한 최적화도 포함된다.

**편의 기능 추가** — 대표적으로 `git switch`와 `git restore` (2019년, v2.23).  
원래 `checkout`이 담당하던 "브랜치 전환"과 "파일 복원"을 역할 분리했다.

```bash
# 예전: checkout이 만능
git checkout main            ← 브랜치 전환
git checkout -- file.js      ← 파일 복원

# 현재: 역할 분리
git switch main              ← 브랜치 전환
git restore file.js          ← 파일 복원
```

**보안 강화** — SHA-1 충돌 취약점(2017년 실증)에 따라 SHA-256 전환이 점진적으로 진행 중.

핵심 구조를 바꾸지 않는 이유는, 전 세계 수억 개의 저장소가 이 구조로 되어 있어  
하위 호환성을 깨뜨릴 수 없기 때문이며, 애초에 설계가 충분히 탄탄하기 때문이다.

---

### 2-4. Git은 누가 관리하고 배포할까?

**관리 주체**

Git은 오픈소스 프로젝트로, 원본 소스는 `git.kernel.org`에서 호스팅된다.

```
공식 소스:   https://git.kernel.org/pub/scm/git/git.git
GitHub 미러: https://github.com/git/git
```

리누스 토르발스가 2005년에 만들었지만, 같은 해에 **주니오 하마노(Junio C Hamano)** 에게  
메인테이너를 넘겼다. 이후 20년간 주니오가 모든 변경 사항의 최종 승인을 담당하고 있다.

개발 방식도 주목할 만하다. Git 개발팀은 **이메일 패치 방식을 지금도 사용**한다.  
Git 메일링 리스트에 패치를 보내고, 리뷰 후 주니오가 머지하는 구조다.  
Git을 만든 사람들이 GitHub의 Pull Request를 안 쓴다는 점이 아이러니하다.

**배포 경로**

원본 소스는 하나지만, 우리 컴퓨터에 도달하는 경로는 OS마다 다르다.

```
Git 공식 소스 코드 (git.kernel.org)
  │
  ├── macOS   → Apple이 Xcode Command Line Tools에 포함 / Homebrew 별도 빌드
  ├── Windows → Git for Windows 프로젝트가 포팅 + 배포 (git-scm.com)
  └── Linux   → 각 배포판 패키지 매니저가 빌드 + 배포 (apt, dnf 등)
```

> 원본 소스는 하나이며, 각 OS 진영이 자기 환경에 맞게 빌드·배포하여 우리 컴퓨터에 도달한다.

---

## 💡 한 줄 정리

> Git은 **스냅샷으로 안전하게 저장**하고, **해시로 중복을 제거**하며, **델타 압축으로 용량을 줄인다.**  
> 이 탄탄한 설계 위에 GitHub·GitLab이 웹 협업을 얹었고, 20년간 커뮤니티가 함께 발전시켜왔다.

---

## 📚 참고

- [Git 공식 문서 - Git Internals: Git Objects](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects)
- [Git 공식 문서 - Packfiles](https://git-scm.com/book/en/v2/Git-Internals-Packfiles)
- [Git 공식 소스 코드](https://git.kernel.org/pub/scm/git/git.git)